apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applies: ServiceAccount, MCPServer, HTTPRoute, MCPServerRegistration
# Does NOT include authpolicy.yaml — it requires envsubst for SSO_ISSUER_URL.
# Apply separately: envsubst < manifests/authpolicy.yaml | kubectl apply -f -
resources:
  - insights-mcp.yaml
  - insights-mcp-registration.yaml
